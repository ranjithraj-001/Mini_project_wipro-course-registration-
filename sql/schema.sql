-- STUDENT table
CREATE TABLE STUDENT (
STUDENT_ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
NAME VARCHAR2(100),
EMAIL VARCHAR2(100),
PRIMARY KEY (STUDENT_ID)
);


-- COURSE table
CREATE TABLE COURSE (
COURSE_ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
TITLE VARCHAR2(200),
CREDITS NUMBER,
PRIMARY KEY (COURSE_ID)
);


-- ENROLLMENT table
CREATE TABLE ENROLLMENT (
ENROLL_ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
STUDENT_ID NUMBER,
COURSE_ID NUMBER,
PRIMARY KEY (ENROLL_ID),
FOREIGN KEY (STUDENT_ID) REFERENCES STUDENT(STUDENT_ID),
FOREIGN KEY (COURSE_ID) REFERENCES COURSE(COURSE_ID)
);


-- sample data
INSERT INTO COURSE (TITLE, CREDITS) VALUES ('Intro to Databases', 4);
INSERT INTO COURSE (TITLE, CREDITS) VALUES ('Data Structures', 3);
INSERT INTO COURSE (TITLE, CREDITS) VALUES ('Operating Systems', 3);


COMMIT;